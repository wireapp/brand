{
  "name": "brand",
  "license": "LicenseRef-LICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/wearezeta/brand.git"
  },
  "devDependencies": {
    "husky": "1.1.2",
    "lint-staged": "8.0.3",
    "prettier": "1.14.2"
  },
  "lint-staged": {
    "*.{json,md,scss,yaml,yml}": [
      "prettier --write",
      "git add"
    ]
  },
  "scripts": {
    "fix": "prettier --write \"**/*.{json,md,scss,yaml,yml}\""
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }
}
